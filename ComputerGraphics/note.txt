1.目前导入的模型，要么每个部件都有贴图，要么每个部件都没有贴图，否则会出问题
（有贴图部件的贴图会跑到没贴图部件上）。可以修改一下适应部分有贴图的模型，但
目前暂时用不到而且有点麻烦就先不改。（而且可以修改模型mtl文件避免这个问题）
2.光照，着色器里三种光照我都加了，默认只先设了个平行光，到时候要统一一下光照
3.model矩阵，目前画模型先由main函数给model矩阵，到时候模型和布局确定了就直接写进去。
4.fragmentshader里有贴图模型的光照我把贴图和材质颜色一起乘上去了，不懂对不对，目前
看上去还行(大多数免费的模型做的太不精细了，很多属性都没设，我手加了一下）
5.stb_image_need.cpp我删了，不然一直报错。。。要用stb_image最好还是
#ifndef STB_IMAGE_IMPLEMENTATION
#define STB_IMAGE_IMPLEMENTATION
#include "lib/stb_image.h"
#endif // !STB_IMAGE_IMPLEMENTATION
？
6.Include/assimp 为assimp需要的头文件 Common/lib 里的assimp-vc142-mtd.lib和
Debug里的assimp-vc142-mtd.dll也是assimp要用的